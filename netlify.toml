[build]
  publish = "public"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# /api/* is handled by Netlify Functions (see config.path inside each function).
# Static files in public/ are served for everything else.
